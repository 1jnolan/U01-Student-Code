#File name U01.55.1 Lists - entering items into a list.py
#*******************************************************
#Enter your Name:
#*******************************************************

#1. Create an empty list to store all of the pets names
petList=[]

#2. Ask the user how many pets they own, store their answer in a variable
totalPets=int(input("How many pets do you have?"))


#3. Some people may have no pets, we must take this into account.
#if they pets we will ask them for the name of each pet.
#else - if they have 0 pets see the last 2 lines of code. 

if totalPets>0:
    for pet in range (totalPets): #examine what "in range" does in this code.
        name=input("Enter pet's name)
        #4. Once we have the pets name stored in the variable "name" we must add it to the list
        petList.append(name)
    
    #5. Once we have all of the users pets names we can print them all back to the user:
    print("The names of your pets are:)
    #To do this we will use a FOR LOOP
    for pet in range(totalPets):
        print(petList[pet])
        #note [pet] on line 27. this is telling the print function to print the 
        #item in the list at location [pet] run the code and investigate how it works
        


else:
    print("Why don't you get a pet?!")
